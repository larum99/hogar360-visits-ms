server:
  port: 8092

spring:
  application:
    name: hogar360-visits-ms
  datasource:
    url: jdbc:mysql://localhost:3306/hogar360_visits
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    database: MYSQL
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update

springdoc:
  api-docs:
    path: /api-docs
    enabled: true
  swagger-ui:
    path: /swagger-ui
    enabled: true

security:
  jwt:
    secret: ${JWT_SECRET_KEY}
    expiration-millis: ${JWT_EXPIRATION_MILLIS}

houses:
  service:
    url: http://localhost:8090